package com.ardhian.callmonitoring;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

// keep this simple so test can run without real PostgreSQL
class CallmonitoringApplicationTests {

    @Test
    void basicSanityCheck() {
        // just make sure test class can run
        assertTrue(true);
    }
}
